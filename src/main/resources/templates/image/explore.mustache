{{>layout/header}}
<main id="profile">
	<div class="profile__photo-grid">

		<!-- 유저가 업로드한 Feed 사진 -->
		{{#profileDto.images}}
		<div class="profile__photo">
			<a href="#"> <img src="/upload/{{imageUrl}}">
			</a>
			<div class="profile__photo-overlay">
				<span class="profile__photo-stat"> <i class="fa fa-heart"></i>
					{{likeCount}}
				</span> <span class="profile__photo-stat"> <i class="fa fa-comment"></i>
					{{commentCount}}
				</span>
			</div>
		</div>
		{{/profileDto.images}}

	</div>

	<div class="profile__overlay">
		<i class="fa fa-times"></i>
		<div class="profile__overlay-container">
			<a href="#" class="profile__overlay-link">Change password</a> <a
				href="#" class="profile__overlay-link" id="logout">Log Out</a> <a
				href="#" class="profile__overlay-link" id="cancel">Cancel</a>
		</div>
	</div>

	<script>
// 		function follow(toUserId) {

// 			let data = {
// 				id : toUserId
// 			}

// 			$.ajax({

// 				type : "post",
// 				url : "/follow",
// 				contentType : "application/json; charset=utf-8",
// 				data : JSON.stringify(data),
// 				dataType : "text"

// 			}).done(function(resp) {

// 				if (resp == 1) {
// 					location.href = "/user/" + toUserId;

// 				} else {
// 					alert("팔로우실패");
// 				}

// 			}).fail(function(error) {

// 				alert(error);

// 			});

// 		}

// 		function unFollow(toUserId) {

// 			let data = {
// 				id : toUserId
// 			}

// 			$.ajax({

// 				type : "delete",
// 				url : "/follow",
// 				contentType : "application/json; charset=utf-8",
// 				data : JSON.stringify(data),
// 				dataType : "text"

// 			}).done(function(resp) {

// 				if (resp == 1) {
// 					location.href = "/user/" + toUserId;

// 				} else {
// 					alert("언팔로우실패");
// 				}

// 			}).fail(function(error) {

// 				alert(error);

// 			});

// 		}

// 		$(function() {

// 			//이미지 클릭시 업로드창 실행
// 			$('#profile_image').click(function() {
// 				$("input[name='profileImage']").click();
// 			});

// 			//업로드 파일체인지가 됬을경우 실행되는 이벤트  form태그에 fileProfile은 hidden으로 넣어줌
// 			$("input[name='profileImage']").change(function(e) {
// 				$("#frm_profile_img").submit();
// 			});

// 		})
	</script>

	{{>layout/footer}}